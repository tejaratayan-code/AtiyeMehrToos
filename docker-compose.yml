version: '3.8'
services:
  bot:
    build: .
    env_file: .env
    volumes:
      - .:/app
    restart: unless-stopped
  miniapp:
    build: ./miniapp
    ports:
      - "8000:8000"
    env_file: .env